# Disabled pending TypeScript migration
# This workflow discovers status pages for ski resorts using AI
# Requires: Serper API, OpenAI API
# TODO: Reimplement in TypeScript

name: Discover Status Pages (Disabled)

on:
  workflow_dispatch:
    inputs:
      enabled:
        description: 'Enable this workflow (set to true)'
        required: true
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

jobs:
  discover:
    name: Discover Resort Status Pages
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.enabled == 'true' }}
    permissions:
      contents: write

    steps:
      - name: Workflow Disabled
        run: |
          echo "⚠️  This workflow is disabled pending TypeScript migration"
          echo ""
          echo "This workflow previously used Python scripts to discover resort status pages."
          echo "It needs to be reimplemented in TypeScript with:"
          echo "  - Serper API integration for web search"
          echo "  - OpenAI API integration for page analysis"
          echo ""
          echo "To enable this workflow:"
          echo "  1. Implement scripts/discover-status-pages.ts"
          echo "  2. Update this workflow file"
          echo "  3. Add required secrets: SERPER_API_KEY, OPENAI_API_KEY"
          exit 1
