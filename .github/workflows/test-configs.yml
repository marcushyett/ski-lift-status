name: Test Resort Configs

on:
  workflow_dispatch:
    inputs:
      resort_id:
        description: 'Resort ID to test (leave empty for all)'
        required: false
        default: ''
  schedule:
    # Run daily at 6 AM UTC
    - cron: '0 6 * * *'

jobs:
  test-configs:
    name: Test Resort Configurations
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run config tests (all resorts)
        run: |
          cd src/ski_lift_status/configs
          node runner.js --all > ../../../test-output.txt 2>&1 || true
          cat ../../../test-output.txt

      - name: Parse results and create summary
        run: |
          echo "## 🎿 Resort Config Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Extract summary from output
          if grep -q "=== SUMMARY ===" test-output.txt; then
            echo "### Summary" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            sed -n '/=== SUMMARY ===/,$p' test-output.txt >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ No summary found in output" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            test-output.txt
            test-results.json
          if-no-files-found: ignore
